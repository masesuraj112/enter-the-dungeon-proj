/** This is a Weapon interface which describes how enemies of bulletKinType
 * handle firing fireballs
 */
public interface Weapon {
    /** Template for update method
     * @param player inputs a player
     */
    void update(Player player);
}
